Prometheus exporter {{ .Release.Name }} version  {{ .Chart.Version }} has been installed.

A ServiceMonitor has been created, so Prometheus should pick up on this automatically.

And if AlertManager is configured correctly (outside the realm of this
chart, sorry), you should get alerts for high/low blood sugar levels.
